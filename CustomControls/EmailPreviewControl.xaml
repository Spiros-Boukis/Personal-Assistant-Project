<UserControl x:Class="Personal_Assistant.CustomControls.EmailPreviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:local="clr-namespace:Personal_Assistant.CustomControls"
             mc:Ignorable="d" 
             Height="auto" Width="auto">
    <Grid>
        <StackPanel Orientation="Vertical" Width="auto" >
            <Label FontSize="23" FontWeight="Bold"  Content="{Binding From}" Width="auto" Height="35"/>
            <Label FontSize="20" FontWeight="Bold"  Content="{Binding Title}" Width="auto" Height="35"/>
            <TextBox FontSize="13" MinWidth="500" x:Name="newEntryText" Height="70"  Text="{Binding Body}"></TextBox>
        </StackPanel>
    </Grid>
</UserControl>
